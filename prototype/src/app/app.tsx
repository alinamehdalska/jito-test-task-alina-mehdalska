/** Placeholder shell until the router lands; exercises the generated theme end to end. */
export function App() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-12 bg-bg-canvas p-20">
      <section className="rounded-20 bg-bg-surface p-20 shadow-xs">
        <h1 className="type-large-title text-text-primary">Plate</h1>
        <p className="type-subhead text-text-secondary">Coded prototype — shell coming next.</p>
      </section>
      <div className="h-control-cta rounded-full bg-bg-surface/65 px-24 backdrop-blur-glass" />
    </main>
  );
}
